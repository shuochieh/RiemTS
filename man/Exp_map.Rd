% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifold.R
\name{Exp_map}
\alias{Exp_map}
\title{Exponential map on a manifold}
\usage{
Exp_map(mfd, v, mu)
}
\arguments{
\item{mfd}{a manifold object}

\item{v}{a single tangent vector or a batch of tangent vectors, at mu}

\item{mu}{base point}
}
\value{
a point, or a batch of points, in the same shape as `v`
}
\description{
Maps a tangent vector `v` at base point `mu` back onto the manifold.
Inverse of [Log_map()].
}
\examples{
mfd = manifold_sphere()
mu = c(1, 0, 0)
x = c(0, 1, 0)
v = Log_map(mfd, x, mu)   # tangent vector at mu pointing towards x
Exp_map(mfd, v, mu)       # back to (approximately) x
}
